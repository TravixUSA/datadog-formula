{# This is the couchbase integration template #}
{%- from slspath + "/map.jinja" import datadog with context %}
{%- set integrations = datadog.get('integrations', {}) %}
init_config:

instances:
  - server: {{ integrations.couchbase.server }}
    user: {{ integrations.couchbase.username }}
    password: {{ integrations.couchbase.password }}
    # timeout: 10 # Optional timeout to http connections to the API
    # tags:
    #   - optional_tag1
    #   - optional_tag2
